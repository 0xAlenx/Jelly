server {
    listen 80;
    server_name app.jellyai.cloud;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    location / {
        return 302 http://gateway.jellyai.cloud/app;
    }
}
